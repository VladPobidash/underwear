import React from "react";

const Category = ({ categoryName }) => {
  return (
     <>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     <div>{categoryName}</div>
     </>
  );
};

export default Category;
